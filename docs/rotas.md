# Rotas da API

A API possui rotas fictícias para manipulação de livros.

GET /livros
Retorna a lista de livros cadastrados.

POST /livros
Adiciona um novo livro ao sistema.

PUT /livros/:id
Atualiza informações de um livro.

DELETE /livros/:id
Remove um livro da biblioteca.
